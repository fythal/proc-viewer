<h1><%= t(:login) %></h1>

<%= form_for(@login) do |f| %>
<% if @login.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@login.errors.count, "error") %> prohibited this ann from being saved:</h2>

  <ul>
    <% @login.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>

<div class="field">
  <%= f.label :user_id %><br>
  <%= f.select :user_id, @users.map { |u| [u.name, u.id] }, { include_blank: true } %>
</div>
<div class="field">
  <%= text_field_tag "login[new_user_name]" %>
</div>
<div class="actions">
  <%= f.submit %>
</div>
<% end %>
