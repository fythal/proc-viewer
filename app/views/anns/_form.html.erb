<%= form_for(@ann) do |f| %>
  <% if @ann.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ann.errors.count, "error") %> prohibited this ann from being saved:</h2>

      <ul>
      <% @ann.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :panel_number %><br>
    <%= f.text_field :panel_number %>
  </div>
  <div class="field">
    <%= f.label :panel_location %><br>
    <%= f.text_field :panel_location %>
  </div>
  <div class="field">
    <%= f.label :procedure %><br>
    <%= f.file_field :procedure %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
