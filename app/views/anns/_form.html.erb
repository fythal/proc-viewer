<%= form_for(@ann) do |f| %>
  <% if @ann.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ann.errors.count, "error") %> prohibited this ann from being saved:</h2>

      <ul>
      <% @ann.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :pdf %><br>
    <%= f.text_field :pdf %>
  </div>
  <div class="field">
    <%= f.label :panel, "パネル番号" %><br>
    <%= f.text_field :panel %>
  </div>
  <div class="field">
    <%= f.label :window, "窓番号" %><br>
    <%= f.text_field :window %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
